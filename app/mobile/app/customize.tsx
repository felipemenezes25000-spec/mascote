import { Redirect } from 'expo-router';

/** Rota legada — personalização completa em settings/personalization. */
export default function Customize() {
  return <Redirect href="/settings/personalization" />;
}
